apiVersion: v1
kind: Pod
metadata:
  name: cleanup-pod
  namespace: plex
spec:
  containers:
  - name: sqlite
    image: alpine:latest
    command: ["sleep", "3600"]
    volumeMounts:
    - mountPath: "/mnt/data"
      name: postgres-storage
  volumes:
  - name: postgres-storage
    persistentVolumeClaim:
      claimName: plex-config
  restartPolicy: Never
